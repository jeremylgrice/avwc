import { CMS_NAME } from '../lib/constants'

const Intro = () => {
  return (
    <section className="flex-col md:flex-row flex items-center md:justify-between mb-20 mt-8 md:mb-12" role="banner">
      <h1 className="text-4xl font-bold tracking-tight md:tracking-tighter leading-tight">
        Vet That Codes
      </h1>
    </section>
  )
}

export default Intro
